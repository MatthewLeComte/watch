{
  "name": "watch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "watch — private movie library on Cloudflare",
  "scripts": {
    "build": "npx wrangler deploy --dry-run --outdir=dist",
    "deploy": "echo 'Deploy via Workers Builds only'",
    "versions:upload": "echo 'Deploy via Workers Builds only'",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "engines": { "node": ">=20" },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^5.6.0",
    "wrangler": "4.114.0"
  }
}
